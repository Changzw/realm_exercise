# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def common_pods
  pod 'RealmSwift', '~> 4.3'
  pod 'Realm', '~> 4.3'
end

target 'Backlink' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Backlink
  common_pods
end

target 'Encryption' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Encryption
  common_pods
end

target 'GroupedTableView' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GroupedTableView
  common_pods
end

target 'Migration' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Migration
  common_pods
end

target 'Simple' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Simple
  common_pods
end

target 'TableView' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TableView
  common_pods
end
